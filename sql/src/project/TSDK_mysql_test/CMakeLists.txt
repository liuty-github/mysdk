INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)
#AUX_SOURCE_DIRECTORY(. SRC_DIR)

SET(SRC_LIST main.cpp)

ADD_EXECUTABLE(TSDK_mysql_test ${SRC_LIST})
TARGET_LINK_LIBRARIES(TSDK_mysql_test rt)
TARGET_LINK_LIBRARIES(TSDK_mysql_test TSDK_mysql)
TARGET_LINK_LIBRARIES(TSDK_mysql_test dl)

